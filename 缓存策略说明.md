# 缓存策略说明

> 2026-05-11 确立。覆盖施工基准中尚未涉及的客户端缓存与跨重启记录恢复。

## 一、客户端 localStorage 7 天滚动缓存

### 存储结构

key: `yomi_job_history`
value: `JobHistoryEntry[]` JSON 数组

```typescript
interface JobHistoryEntry {
  job_id: string;
  file_name: string;
  questions_count: number;
  status: string;       // "uploaded" | "completed" | "failed"
  created_at: string;   // ISO 8601
}
```

### 写入时机

| 时机 | 内容 |
|------|------|
| 上传成功（POST /api/parse-jobs → 200） | 写入 `status: "uploaded"`, `questions_count: 0` |
| 识别完成（status === "completed"） | 更新 `status: "completed"`, `questions_count` 为实际值 |

### 过期策略 — 滚动 7 天窗口

每次读写前执行 `filter(e => now - created_at < 7天)`：
- 第 8 天自动清除第 1 天的记录
- 第 9 天清除第 2 天的，以此类推
- 始终保留最近 7 天数据

### 恢复逻辑

页面进入 idle 状态时：
1. 取 `history[0]`（最新一条）
2. 若 `status !== "completed" && status !== "failed"`
3. 且距 `created_at` < 24 小时
4. → 自动 `router.replace` 到该 job，显示 ProcessingStatus

### 手动清除

右上角「清除」按钮 → `localStorage.removeItem("yomi_job_history")` → 清空列表。
清除后自动从后端 API 拉取云端记录作为补充展示。

## 二、后端 DB 持久化（跨重启）

### 表结构

```sql
CREATE TABLE IF NOT EXISTS parse_jobs (
    job_id TEXT PRIMARY KEY,
    child_id TEXT NOT NULL DEFAULT '',
    parent_id TEXT NOT NULL DEFAULT '',
    file_name TEXT DEFAULT '',
    questions_count INTEGER DEFAULT 0,
    status TEXT DEFAULT 'uploaded',
    created_at TEXT DEFAULT (datetime('now'))
);
```

### 写入时机

| 时机 | 调用 |
|------|------|
| `create_parse_job` 注册任务后 | `_db.save_parse_job(jid, child_id, parent_id, file_name, 0, "uploaded", now)` |
| `save_result` 保存结果时 | `_db.save_parse_job(jid, child_id, parent_id, file_name, len(questions), status, now)` |

### 查询接口

`GET /api/parse-jobs/recent` 合并策略：
1. 先取内存 `_jobs`（更新鲜）
2. 再取 DB `parse_jobs`（跨重启）
3. 按 `job_id` 去重，按 `created_at` 倒序
4. 返回最近 10 条

### 设计原则

- 内存优先：运行中数据不查 DB，DB 仅作重启恢复
- 写入容错：DB 写入失败不阻塞主流程（已有 try-except 包裹）
- 前端 localStorage 为主力，API 为补充（清除本地缓存后从云端恢复）

## 三、前端组件

### useJobHistory hook

`src/hooks/useJobHistory.ts`
- `history: JobHistoryEntry[]` — 响应式 state，自动加载 + 滚动清理
- `upsert(entry)` — 插入或更新一条记录，触发状态更新 + localStorage 写入
- `clearAll()` — 清空 localStorage + state

### 展示逻辑

workspace 页面 idle 状态下的「历史记录（最近 7 天）」区域：
- 过滤 `status === "completed"` 的记录
- 按 `created_at` 倒序
- 每条可点击跳转到对应 job
- 无本地记录时展示 API 返回的云端记录
