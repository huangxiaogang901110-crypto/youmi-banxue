# 切题算法说明

> 2026-05-11 整理。对应后端 `question_cutter.py`，基准 §14「OCR block + 题号规则 + 版面规则生成候选切块」。

## 一、输入输出

| 输入 | 输出 |
|------|------|
| 阿里云 OCR `RecognizeAllText` 返回的 blocks 列表 | `QuestionGroup[]` / 前端可用的题目列表 |

```python
# 输入格式
[{"text": "1. 4+7=", "pos": [x, y, w, h]}, ...]

# 输出格式
[{
    "question_number": 1,
    "question_text": "1. 4+7= 7+5= ...",
    "bbox": [min_x, min_y, width, height],
    "blocks_count": 3,
}, ...]
```

## 二、算法流程

### 第一步：块排序

所有 OCR 块按 `(y, x)` 坐标排序——先从上到下，再从左到右。

### 第二步：题号锚点扫描

对每个 OCR 块的文本开头做正则匹配，支持 4 种题号模式：

| 优先级 | 模式 | 正则 | 示例 |
|--------|------|------|------|
| 1 | 括号小题 | `^[（(](\d+)[）)]` | `(1)`, `（3）` |
| 2 | 圈号 | `①②③④⑤⑥⑦⑧⑨⑩` | `①`, `⑤` |
| 3 | 阿拉伯数字 | `^(\d+)\s*[.、．)\s]+` | `1.`, `2、`, `3)` |
| 4 | 中文大题 | `^([一二三四五六七八九十]+)\s*[、．\s]+` | `一、`, `十一、` |

### 第三步：锚点分组

找到的锚点作为题目边界：
- 锚点索引之间的所有 OCR 块归为一个题目组
- 锚点之前的块（如页眉、标题）→ 归入前置组（`question_number = 0`）
- 每个题组的 `bbox` 为组内所有块的 union 矩形

### 第四步：无锚点 Fallback

若未检测到任何题号 → 按 y 方向间距分组：
- 相邻两个块的 y 间距 > `gap_threshold`（默认 40px）
- 视为题目边界
- 题号从 1 开始递增

## 三、关键设计决策

### 禁止大模型画框

切题完全基于 OCR 坐标 + 正则规则，**不将图片发送给大模型做 bbox 预测**。

理由：
- OCR 已返回精确坐标，规则足够
- 大模型画框有幻觉风险（多画/少画/偏移）
- 节省 token 和延迟

### 中文数字映射

```
一→1  二→2  三→3  四→4  五→5
六→6  七→7  八→8  九→9  十→10
十一→11  十二→12  ...  十九→19
```

仅支持 1-19，超过取不到则 `None`（退化为无锚点 fallback）。

## 四、调用位置

`backend/main.py` → `worker_process_job`：

```python
# Stage: cutting
_jobs[jid]["job"].status = JobStatus.cutting
cut_results = cut_to_questions(extracted["blocks"])
```

在 OCR 完成后、Qwen-VL vision review 之前执行。

## 五、约束与边界

| 约束 | 说明 |
|------|------|
| 前置组 | `question_number = 0`，前端不展示为题目 |
| 最小文本 | 前置组中块文本 ≤ 2 字符则跳过 |
| bbox 格式 | `[x, y, w, h]` 整数值 |
| 并发安全 | 无共享状态，纯函数，可并行 |
