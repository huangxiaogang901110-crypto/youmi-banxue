# 悠米伴学 — 项目级开发规则

> 本文件约束所有在悠米项目下的工作行为。
> 收到悠米相关任务时，Hermes 必须先读本文件，再读全局规则。

## ⛔ 最高原则：工作目录确认

**任何代码增、查、改、删操作前，必须先确认当前工作目录。**

```
① 执行任何文件操作前 → 确认 pwd 在 ~/yomi-dev/ 下
② 如不在开发区 → 停止，cd 到 ~/yomi-dev/ 再操作
③ 绝不直接在 ~/yomi/（公共区）修改文件
```

## 区域规则

| 区域 | 路径 | 权限 |
|------|------|------|
| **开发区** | `~/yomi-dev/` | ✅ 可读可写（唯一工作区） |
| **公共区** | `~/yomi/` | ❌ 只读，禁止修改 |

## Git 操作强制前置

**任何悠米项目的 git 操作前，必须先读公共区 git 规则文件：**

```bash
cat ~/yomi/GIT_SYNC_RULES.md
```

该文件定义了 hermes_me 与 hermes_colleague 的标准同步流程、冲突处理、GitHub 推送权限及禁止清单。

## 工作流程

```
开发任务 → cd ~/yomi-dev/ → 修改 → 验证 → 停下等指令
                                             ↓
                               用户说「同步」 → 同步到公共区
                               用户说「推」   → git push 到 GitHub
                               合并走 GitHub 核验，不得无脑覆盖
```

**⛔ 禁止自动同步、禁止自动 push。** 必须等用户明确说「同步」「推」「上传」等指令后才能执行。

## 禁则

- ❌ 在 ~/yomi/ 下执行 write_file / patch / terminal(写操作)
- ❌ 不确认 pwd 就直接开工
- ❌ 绕过 GitHub 核验直接 rsync 到公共区
- ❌ 未经用户明确许可 push 至 GitHub 公共仓库
- ❌ 未经用户说「同步」自动将开发区改动同步到公共区
