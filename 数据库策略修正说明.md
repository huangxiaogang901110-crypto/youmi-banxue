# 数据库策略修正说明

> 2026-05-10 | 覆盖《悠米伴学-施工基准文档御三家GPT终审版》中数据库相关策略

## 背景

施工基准编写时项目尚未部署，假设 Phase 0 在本地 Mac 开发、Phase 1/2 逐步上云。基准据此规划了「本地 SQLite → 云端 PostgreSQL」的迁移路径。

实际部署后，项目运行在阿里云 ECS（`39.107.119.136`）。**ECS 系统盘本身就是阿里云 ESSD 云盘**，不是本地物理盘。数据已持久化在云上，ECS 关机/重启不丢失。

## 修正结论

**SQLite + ECS 系统盘（阿里云 ESSD）= 最终持久化方案。不再需要迁移到 PostgreSQL/Supabase。**

## 被覆盖的基准条款

以下基准原文中的策略描述以本说明为准：

| 基准原文 | 修正 |
|---------|------|
| "Phase 0 本地 Demo / SQLite 或本地 PostgreSQL" | 当前即为云上运行，非 Demo。SQLite 在 ESSD 云盘上，已是云存储。 |
| "FastAPI 云部署 + PostgreSQL/Supabase" | 无需迁移。SQLite + ESSD 足够支撑当前及预期用户规模。 |
| "Phase 1/2 通过配置切换为 PostgreSQL" | 取消。如未来确需水平扩展或高并发，再评估 RDS；当前无需预留。 |
| "Supabase Free 只能用于 Demo/内测" | 不适用。不使用 Supabase。 |
| 所有「PostgreSQL 必须」「PostgreSQL 兼容语法」类约束 | 降级为建议。Schema 继续用 Alembic 管理，但语法以 SQLite 为准。 |

## 保留不变的条款

以下基准条款仍然有效：

- 数据库只存结构化数据，原图/PDF/裁切图不入库（走对象存储）
- API Key、数据库连接串通过环境变量注入，不进 Git
- Alembic 管理 schema 迁移
- enqueue / worker / save 三边界函数分离
- model_call_log 按 30-90 天归档，ai_tutoring_chat 跟随错题生命周期
- 云端主账本 + 手机本地缓存（IndexedDB）边界不变

## 当前数据库架构

```
ECS 系统盘 (阿里云 ESSD 80G, 65G 可用)
  └── /home/hermes_me/yomi/backend/yomi.db  (SQLite)
       ├── model_call_log    — AI 调用日志
       ├── parse_job         — 作业解析任务
       ├── question_item     — 题目明细
       ├── tutor_chats       — 辅导对话
       └── credit_balances   — 学豆余额
```

后端写入时内存 + DB 双写。重启后从 DB 恢复内存状态。ESSD 云盘保证 ECS 生命周期外数据不丢（前提：不主动初始化系统盘）。
