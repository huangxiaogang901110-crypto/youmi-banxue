# 悠米伴学 — 基准更新摘要
## 2026-05-11 最新变更（供 GPT 参考）

---

## 1. 登录页重构

### 设计驱动
- 用 Qwen-VL Max 分析了登录页基准设计图
- 基准图要素：Logo + IP 形象 + 双 Tab（密码/验证码）+ 社交登录 + 协议勾选 + 移动端优先

### 落地内容
- **Logo**：用户提供的 PNG Logo 放入 `public/logo.png`（2048×2048 RGBA）
- **布局**：品牌区（Logo + "AI家庭学习助手" 标语）+ 表单卡片，背景渐变 `primary-light/60 → background`
- **双 Tab 切换**：密码登录 | 验证码登录，`bg-muted` 分段控件样式
- **手机号输入**：+86 前缀 + 独立 label + focus ring
- **验证码**：获取按钮 60s 倒计时（接口 TODO，占位）
- **协议勾选**：必须勾选才能点登录按钮
- **社交登录**：微信/家长灰色禁用占位 + 游客可用（直接 setToken("guest-token") 进入）
- **构建验证**：`/login/` 返回 200，全量路由 200，404 正常

### 技术决策
| 决策点 | 裁定 |
|--------|------|
| Logo | 使用用户提供的唯一 PNG |
| 社交登录 | 微信/家长放占位（disabled），游客可进入 |
| 登录方式 | 密码登录 + 验证码登录并存，Tab 切换 |

---

## 2. 左划删除（SwipeableCard 组件）

### 问题根因（经过 4 轮实战验证）
1. **React 合成 touch 事件不可靠** — `onTouchStart/Move/End` 在 Next.js 静态导出 + Android Chrome / iOS Safari 上完全不触发
2. **`<a>` 标签拦截** — `<Link>` 渲染为 `<a href>`，浏览器接管触摸事件
3. **数据源不对称** — 同一页面两套数据源（localStorage `completedHistory` + 服务器 API `apiOnly`），条件判断只覆盖一套

### 唯一可靠实现
- 抽取为 `src/components/common/SwipeableCard.tsx` 独立组件
- 使用 **原生 DOM `addEventListener('touchstart/touchend')`**，完全绕过 React 合成事件系统
- 展开阈值：左划 > 40px；关闭：右划 > 10px 或轻触
- CSS `touchAction: pan-y` 区分垂直滚动和水平滑动
- 双按钮：取消（蓝色 64px）+ 删除（红色 64px）

### 禁用项
- ❌ React 合成 `onTouchStart`/`onTouchMove`/`onTouchEnd`
- ❌ `<Link>` 或 `<a>` 标签放在可滑动区域内
- ❌ touchmove 中调 `e.preventDefault()`

---

## 3. 缓存清除策略完善

- `useHomeworkDays` 新增 `clearAll`、`removeDay`
- `useJobHistory` 新增 `removeEntry`
- `localCache.ts` 的 `clearAllCache` 扩展为覆盖 localStorage 全部业务键
- 历史记录区改为 SwipeableCard 左划删除
- `apiOnly`（服务器已清除的过期记录）不展示，仅保留本地存活记录

---

## 4. 经验提炼（已写入规则文档）

| # | 经验 | 写入位置 |
|---|------|---------|
| 1 | 诊断标记法 — 加可见 marker 确认事件是否触达 | `systematic-debugging` §8 |
| 2 | React 合成 Touch 事件在移动端不可靠 | `ui-ux-pro-max` §2 |
| 3 | `<a>`/`<Link>` 标签捕获触摸事件 | `ui-ux-pro-max` §2 + `AGENTS.md` §14 |
| 4 | 双数据源渲染不同组件的条件必须放宽到 OR | `baseline-driven-dev` Agent 默认行为表 |

---

## 5. 多模块开发偏好（2026-05-11 确认）

按指定顺序（如 B→C→A）连续开发，不每轮等确认。每个模块自己走完 写代码→重启→构建→自测→push 全流程后直接开下一轮。只在遇到需要用户决策的阻塞点才停下。

---

## 6. 登录基准图

- 存于 ECS 项目根目录 `login-baseline.jpg`（不入 git）
- 基准图经 Qwen-VL 分析，分析结果见 §1

---

## 当前项目状态快照

- **Phase**：Phase 0 全部完成 ✅
- **构建**：Next.js 16.2.6，7 路由全静态导出，零错误
- **部署** ECS 39.107.119.136，Nginx + FastAPI :8000
- **最近提交**：`159512a` feat: 登录页重构
- **设计 Token**：主色 `oklch(0.68 0.12 180)` 蓝绿调，品牌色 `#4DBBAA`
- **需完成的下步**：登录页社交登录占位→真实接入
