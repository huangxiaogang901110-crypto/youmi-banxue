# 识别管线说明 — Qwen-VL 优先

> 2026-05-11 更新。对应后端 `main.py` worker + `vision_client.py`。

## 一、管线架构

```
上传图片
    │
    ▼
Qwen-VL 全图识题 (extract_questions)  ←── 优先
    │              │
    ▼              ▼ 失败/不可用
直接出题列表    OCR → 切题算法 ──→ 逐题 Qwen-VL 复审
    │              │
    └──────┬───────┘
           ▼
     Schema 校验 → 保存
```

## 二、两种路径对比

| | Qwen-VL 优先（新） | OCR 回落（旧） |
|---|---|---|
| 模型 | `qwen-vl-max` | 教育 OCR + `qwen-vl-max` |
| API 调用数 | **1 次** | 1(OCR) + N(逐题 Qwen-VL) |
| 延迟（20题） | ~21s | ~30-50s（串行） |
| 成本（20题） | **~¥0.011** | ~¥0.057 |
| 坐标精度 | 无精确 bbox | OCR 原生坐标 |
| 适用场景 | 数学口算/标准题目 | 复杂排版/需要 bbox |

## 三、成本分析

以 20 道口算题为例：

| 路径 | 输入 token | 输出 token | 单价 | 合计 |
|------|-----------|-----------|------|------|
| Qwen-VL 优先 | 1,307 | 618 | ¥3/¥12 per 1M | **¥0.011** |
| OCR 回落 | 22,000 | 4,000 | ¥1.5/¥6 per 1M | ¥0.057 |

> Qwen-VL 优先比旧流程便宜 **~5 倍**，因为只传一次图（省去逐题重复传图的 token）。

## 四、回落触发条件

OCR 路径在以下情况自动激活：
- `QWEN_DASHSCOPE_API_KEY` 未配置或无效
- Qwen-VL API 返回错误
- Qwen-VL 返回空题目列表（JSON 解析失败等）

## 五、模型选择

`qwen-vl-max`（非 `plus`）：
- 教育场景识别更准
- 题目结构理解更强
- 多模态 token 同 `plus`（图片 1232 token 固定）
- 输出单价是 `plus` 的 2 倍，但无需逐题调用，总成本更低

## 六、切题算法定位

当走 Qwen-VL 优先路径时：
- **切题算法完全不执行**
- 题目列表由 Qwen-VL 的 JSON 输出直接生成
- 无 bbox 信息（`bbox = [0, 0, 0, 0]`）

切题算法仅在 OCR 回落路径中运行（详见 `切题算法说明.md`）。
