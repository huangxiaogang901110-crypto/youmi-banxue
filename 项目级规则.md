# 悠米伴学项目级规则

## 1. main.py 定位

`backend/main.py` 已完成模块化拆分，后续只保留：

- app 初始化
- middleware / CORS / limiter
- include_router
- `/health`
- 全局 exception handler

禁止把业务逻辑重新写回 `main.py`。

## 2. 模块职责

| 事项 | 文件 |
|---|---|
| 作业识别接口、上传、状态、历史、题目查询 | `backend/routes/parse_routes.py` |
| 识别后台流程、Qwen/DeepSeek、OCR、判对错 | `backend/pipeline.py` |
| 登录、注册、孩子列表、孩子切换 | `backend/routes/auth_routes.py` |
| 权益、激活码、订单、callback、账单流水 | `backend/routes/entitlement_routes.py` |
| 错题本 | `backend/routes/mistakes_routes.py` |
| 作业清单 | `backend/routes/homework_routes.py` |
| 模型、枚举、请求/响应结构 | `backend/models.py` |
| 当前用户/孩子、鉴权依赖 | `backend/dependencies.py` |
| 全局任务状态、Mock、内存队列 | `backend/job_store.py` |
| 共享限流器 | `backend/limiter.py` |

## 3. 开发规则

常规开发从项目根目录开始：

    cd ~/yomi-dev

后端测试从：

    cd ~/yomi-dev/backend

不要进入 `backend/routes/` 里运行项目或测试；它只是代码分层目录。

每轮开发先判断属于哪个模块，只改对应文件。不得顺手跨模块大改。

## 4. 禁止事项

禁止：

- 在 `main.py` 新增业务路由；
- 在 `main.py` 写数据库 CRUD；
- 在 `main.py` 写模型调用；
- 在 `main.py` 写上传解析流程；
- 在 `main.py` 写登录、权益、错题、作业清单等业务逻辑；
- routes 模块反向 import main；
- 路由拆分和业务修复混在一个 commit；
- 后端修改和前端 UI 修改混在一个 commit；
- 环境/SOCKS/OCR 修复和代码 refactor 混在一个 commit。

## 5. 路由规则

所有业务路由使用 `APIRouter`。

新增或迁移路由时：

- 不随意加 prefix；
- 不改变 API 路径；
- 不改变请求字段；
- 不改变响应字段；
- 不改变状态码；
- 不反向 import main；
- 只补必要 imports。

## 6. 验证规则

后端修改后至少执行：

    cd ~/yomi-dev/backend
    python -m py_compile main.py models.py dependencies.py job_store.py pipeline.py limiter.py routes/*.py
    python -c "import main; print('MAIN_IMPORT_OK')"
    TEST_PWD=123356 ./run-tests.sh

涉及作业识别链路时再执行：

    TEST_PWD=123356 ./run-flow-tests.sh

涉及前端链路时再执行：

    cd ~/yomi-dev
    ./run-e2e-tests.sh
